namespace Itdear.Dominio.Core.Entities
{
    /// <summary>
    /// Raiz comun de las entidades del dominio. Las entidades de este modelo usan
    /// llaves compuestas definidas en el mapeo, por lo que la base no impone ningun
    /// identificador; solo sirve como marcador para las restricciones genericas.
    /// </summary>
    public abstract class Entity
    {
    }
}
